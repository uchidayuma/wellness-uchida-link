# DESIGN_CONSTRAINTS.md — デザイン制約セット（2026-07-24策定）

方向性: **余白＋罫線の和文タイポグラフィ系**。「経営と技術の間に立つ参謀」の落ち着き・信頼感を、装飾ではなく組版で作る。
このファイルは実装者（人間・AI問わず）への**制約**である。実装者はこの範囲内でのみ判断してよい。制約の変更は使用者（打田）の承認が必要。

## 1. 色（合計3値のみ）

| 役割 | 値 | 用途 |
|---|---|---|
| 墨 | `#1f1f1f` | 本文・見出し・ベタ背景（フッター/CTA帯） |
| 紙 | `#fdfcf9` / `#f5f3ee` | 背景2値。セクション区切りは背景切替か罫線で |
| 藍（アクセント） | `#2b4a6f` | リンク・ボタン・番号・強調。**1色のみ** |

- 上記以外の色は禁止（現行のティール/緑は全廃）。罫線は `#d8d5cd`。

## 2. 禁止事項

- `border-radius`（角丸）全面禁止。ボタンも矩形
- `box-shadow`・グラデーション・カードUI（枠で囲んだ小箱の均等並び）禁止
- アイコン・絵文字・イラスト風SVG禁止。構造は**番号（01, 02…）と罫線**で示す
- 3カラム均等グリッド禁止。並列要素は縦積み＋罫線区切り、または非対称2カラム
- アニメーション・ホバー時の浮き上がり禁止（色変化のみ可）

## 3. タイポグラフィ

- 見出し: `"Noto Serif JP", serif`（明朝）。`font-feature-settings: "palt"` で詰める
- 本文: `"Noto Sans JP", sans-serif` weight 400。強調は weight 700（色変更より優先）
- 本文の行長: **1行35〜40字**（`max-width: 38em` 目安）。`line-height: 1.9〜2.0`
- 見出しスケール: 1.25倍率（本文16px基準 → 20 / 25 / 31 / 39px）。過大なヒーロー文字禁止
- 英数字は同ファミリーで統一。装飾フォント禁止

## 4. レイアウト

- コンテンツ最大幅 1040px。セクション上下余白は最低 96px（モバイル 64px）
- 12カラムグリッド上の**非対称配置**を基本形（例: 見出し左5 / 本文右7）。中央揃えの多用禁止（CTA帯のみ可）
- セクション見出しは「番号＋小ラベル＋見出し」の型（例: `01 課題 — この中に、当てはまるものはありませんか？`）
- 料金は表組み（罫線のみ）で提示。プランカードの並列禁止

## 5. コンテンツ原則（軍配・検品対象）

- 実績数字（受注率+5% / -32h / -6h）は**出典注記つきで残す**: 「顧問先1社での実測（2026年）」等。どんな状態からの変化かの文脈を1行添える
- 誇大表現・煽り・恐怖訴求禁止。誠実で言い切るトーン。IT用語は使わない
- 価格は現行ラダーと一致させること。正典は `/Users/uchidayuma/Documents/GitHub/youtube/fractional-cto/strategy/01_positioning.md`（決定ログ・根拠つき）。リポジトリ内ミラーは `strategy/README.md` の料金表（2026-07-24同期済み）
- 特定商取引法に基づく表記ページへの導線をフッターに置く

## 5.5 目視検品で昇格したルール（2026-07-24）

- **濃色背景の上の文字は必ず紙色。** セレクタ詳細度に注意——`.global-nav a` のような文脈側の色指定がボタンの色を上書きしないか、実装後に濃色背景の全ボタン・全リンクを目視確認する
- **日本語の見出し・表の見出し語は文節で折り返す。** `word-break: auto-phrase`（Chrome 119+）＋ `text-wrap: balance` を `:lang(ja)` に適用。「技術の意/思決定」のような語中改行は出荷不可
- **グリッド子要素には `min-width: 0` を入れる。** 子孫に `min-width` 付きの表などがあると列が押し広がり、モバイルでページ全体がはみ出す
- **表の説明列には幅を確保する**（内容列 48%）。1行8〜10字の縦長折り返しは出荷不可
- **目視検品はスクリーンショットで行う**（ヘッドレスChromeは最小幅500pxのため、モバイル検品は500px幅で撮影して判定する。日本語表示は言語シム入り一時ファイルで確認し、検品後に削除する）

## 6. 修正依頼の運用

- 使用者は症状ベースで都度伝えてよい（「ここが窮屈」等）。受けた側が「一回きりの修正」か「ルール化すべき好み」かを仕分け、後者は**修正と同時にこのファイルへ反映**する。制約に載せない個別パッチの積み重ねは禁止（AIっぽさが再発するため）。

## 7. 検品

出荷（デプロイ）前に軍配の検品4種（機械・文体・事実・リーガル）を通す。この制約セット自体への適合も機械検品の対象（禁止プロパティのgrep）。
